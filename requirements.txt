# Django se mantiene en la rama 1.8.x (1.8.19 es el último parche de
# seguridad de esa rama) porque saltar a 2.x implica cambios de código en
# todas las apps (MIDDLEWARE_CLASSES, urls, templates, etc.) - ver el
# apartado de vulnerabilidades pendientes en el readme.
Django==1.8.19
django-autocomplete-light==3.1.8
django-crontab==0.7.1
django-flat-theme==1.1.3
django-flat-responsive==1.0.0
django-smart-selects==1.2.9
# 0.5.14 rompe en Python 3.5 (usa f-strings en lang_BN.py, sintaxis 3.6+)
num2words==0.5.13
# 7.2.0 es la última versión de Pillow con wheel para Python 3.5
Pillow==7.2.0
xlwt==1.3.0
gunicorn==19.10.0
# 2.8.6 es la última versión de psycopg2-binary con wheel para Python 3.5
psycopg2-binary==2.8.6

# Paquetes para uso futuro (comentados):
# mod-wsgi==4.5.7  # Requiere Python 3.5 específico, no recomendado con Gunicorn
# mod-wsgi-httpd==2.4.23.1
# 3.5.52 es la última versión de reportlab que todavía soporta Python 3.5
# (3.5.55+ requiere Python 3.6+)
reportlab==3.5.52  # Para generación de PDFs